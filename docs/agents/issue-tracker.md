# Issue Tracker

We use **GitHub Issues**.

- Use the `gh` CLI to read and write issues
- Never read or write local markdown files for issue tracking
- Do not create Pull Requests unless explicitly asked; push directly to the branch
