# 7. Deployment View

All services are containerized.
- Local Development: `docker compose up --build` spins up all services, RabbitMQ, Redis, and PostgreSQL.
- Production: TODO (Targeting Kubernetes or managed container services like AWS ECS).
