# Release Process

## 1. Preparation
Create a release branch from `develop`.

## 2. Validation
Run manual QA and E2E tests against the release branch.

## 3. Deployment
Merge release branch into `main` and tag the release (e.g., `v1.2.0`).
