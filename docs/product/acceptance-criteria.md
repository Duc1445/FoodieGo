# Acceptance Criteria Guidelines

Acceptance Criteria (AC) must be binary (Pass/Fail) and testable.
- Avoid vague terms ("fast", "easy").
- Use BDD format (Given/When/Then) for complex logic.
- Include edge cases and error states.
