{
  "$id": "https://foodiego.com/schemas/events/food/VariantAddedV1.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "VariantAddedV1",
  "type": "object",
  "properties": {
    "eventId": { "type": "string", "format": "uuid" },
    "eventType": { "const": "VariantAddedV1" },
    "eventVersion": { "const": 1 },
    "occurredAt": { "type": "string", "format": "date-time" },
    "correlationId": { "type": "string", "format": "uuid" },
    "causationId": { "type": "string", "format": "uuid" },
    "aggregateId": { "type": "string", "format": "uuid" },
    "payload": {
      "type": "object",
      "properties": {
        "foodId": { "type": "string", "format": "uuid" },
        "variantId": { "type": "string", "format": "uuid" },
        "sku": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["foodId", "variantId", "sku", "timestamp"],
      "additionalProperties": false
    }
  },
  "required": [
    "eventId", "eventType", "eventVersion", "occurredAt", "correlationId", "causationId", "aggregateId", "payload"
  ],
  "additionalProperties": false
}
